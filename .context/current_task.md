# Current Task Tracking

## Current Objective

Setting up the basic Spring Boot V1 backend architecture and verifying local database connection.

## Micro-Steps

- [x] Download Spring Boot project from Spring Initializr.
- [ ] Configure `application.yml` / `application.properties` with local database credentials.
- [ ] Create `Usuario`, `Obra`, and `Fichaje` JPA entities.
- [ ] Run the application to verify Hibernate auto-creates the tables.

## Blockers / Notes

- Waiting to establish the local database connection parameters.
